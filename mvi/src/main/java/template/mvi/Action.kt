package template.mvi

/**
 * This interface defines intents that occur within our application
 * based on our user's input or application flow.
 *
 * Examples:
 * Based on input: `object SubmitButtonClicked : Action()`
 * Based on a flow: `object LoadProfile : Action()`
 */
interface Action
